# Disabled skills

Skills moved out of `.claude/skills/` to keep them out of the per-message skill listing (each active skill's name + description costs context every message). To re-enable one, move its folder back into `.claude/skills/`; to park one, move it here instead of deleting it.
